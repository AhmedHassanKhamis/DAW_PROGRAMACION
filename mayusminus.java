package random;

public class mayusminus {

	public static void string() {
		
	}
	
	/*
	
	public static void main(String[] args) {
		resultado = nombre.substring (0,1);
		do {
		pos = nombre.indexof ( ', pos)
		if(pos>=0) {
		post+;
		if(nombre.charAt(pos)= ' ")
		resultado = resultado + nombre. substring (pos,pos+1);
		while (pos> =0) ;
		return resultado.toUpperCase ()
		public static String convierteMayusculasEnMinusculasYViceversa ( String
		String resultado "En un lugar de la mancha";
		char caracter = resulttado.charAt (0);
		Character. isUpperCase ( caracter) ;
		*/
	}


